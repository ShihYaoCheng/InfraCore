#include:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Terraform.gitlab-ci.yml
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Terraform/Base.latest.gitlab-ci.yml
# https://gitlab.com/gitlab-org/terraform-images
# https://gitlab.com/gitlab-org/terraform-images/-/blob/master/src/bin/gitlab-terraform.sh
#  - template: Terraform.gitlab-ci.yml

stages:
  - Security
  - plan
  - deploy
  - cleanup

#include:
#  - local: ".gitlab-ci-template.yml"

#TrivyScan-EnvSRE:
#  extends: .TrivyScanTemplate
#  variables:
#    SCAN_PATH: EnvSRE
#    REPORT_FILE: ReportSRE.txt
#
#TrivyScan-Tools:
#  extends: .TrivyScanTemplate
#  variables:
#    SCAN_PATH: tools
#    REPORT_FILE: ReportTools.txt
#
#TrivyScan-Modules:
#  extends: .TrivyScanTemplate
#  variables:
#    SCAN_PATH: Modules
#    REPORT_FILE: ReportModules.txt



# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Terraform/Base.latest.gitlab-ci.yml
# Terraform/Base.latest
#
# The purpose of this template is to provide flexibility to the user so
# they are able to only include the jobs that they find interesting.
#
# Therefore, this template is not supposed to run any jobs. The idea is to only
# create hidden jobs. See: https://docs.gitlab.com/ee/ci/yaml/#hide-jobs
#
# There is a more opinionated template which we suggest the users to abide,
# which is the lib/gitlab/ci/templates/Terraform.latest.gitlab-ci.yml

image:
  # https://gitlab.com/gitlab-org/terraform-images/container_registry/2018822
#  name: registry.gitlab.com/gitlab-org/terraform-images/releases/1.0:v0.31.0
#  name: registry.gitlab.com/gitlab-org/terraform-images/releases/1.0:latest
  # https://gitlab.com/gitlab-org/terraform-images/container_registry/3083041
  name: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2

.plan:
  stage: plan
  variables:
    TF_ROOT: ./
    TF_STATE_NAME: default
  cache:
    key: "${TF_ROOT}"
    paths:
      - ${TF_ROOT}/.terraform/
      - ${TF_ROOT}/.terraform.lock.hcl
  resource_group: ${TF_STATE_NAME}
  artifacts:
    paths:
      # The [gitlab-terraform plan] creates this file.
      - ${TF_ROOT}/plan.cache
    reports:
      # The [gitlab-terraform plan-json] creates this file.
      terraform: ${TF_ROOT}/plan.json
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual


.plan.with.var.file:
  extends: .plan
  script:
    - gitlab-terraform --version
    - gitlab-terraform init
    - gitlab-terraform validate

    # -var-file=filename  Load variable values from the given file, in addition
    #                      to the default files terraform.tfvars and *.auto.tfvars.
    #                      Use this option more than once to include more than one
    #                      variables file.
    # -input=true         Ask for input for variables if not directly set.
    # -out=path           Write a plan file to the given path. This can be used as
    #                      input to the "apply" command.

    # plan_cache="plan.cache"
    # terraform "${@}" -input=false -out="${plan_cache}
    - gitlab-terraform plan -var-file=var_file

    # https://gitlab.com/gitlab-org/terraform-images/-/blob/master/src/bin/gitlab-terraform.sh
    # terraform show -json "${plan_cache}" | jq -r "${JQ_PLAN}" > "${plan_json}"
    # terraform show: Show the current state or a saved plan
    # https://stedolan.github.io/jq/
    # jq- jq is like sed for JSON data - you can use it to slice and filter and map and transform structured data with the same ease that sed, awk, grep and friends let you play with text.
    - gitlab-terraform plan-json -var-file=var_file

.plan.without.var.file:
  extends: .plan
  script:
    - cd ${TF_ROOT}
    - gitlab-terraform --version
    - gitlab-terraform init
    - gitlab-terraform validate

    # -var-file=filename  Load variable values from the given file, in addition
    #                      to the default files terraform.tfvars and *.auto.tfvars.
    #                      Use this option more than once to include more than one
    #                      variables file.
    # -input=true         Ask for input for variables if not directly set.
    # -out=path           Write a plan file to the given path. This can be used as
    #                      input to the "apply" command.

    # plan_cache="plan.cache"
    # terraform "${@}" -input=false -out="${plan_cache}
    - gitlab-terraform plan

    # https://gitlab.com/gitlab-org/terraform-images/-/blob/master/src/bin/gitlab-terraform.sh
    # terraform show -json "${plan_cache}" | jq -r "${JQ_PLAN}" > "${plan_json}"
    # terraform show: Show the current state or a saved plan
    # https://stedolan.github.io/jq/
    # jq- jq is like sed for JSON data - you can use it to slice and filter and map and transform structured data with the same ease that sed, awk, grep and friends let you play with text.
    - gitlab-terraform plan-json


#============================#
# .apply                     #
#============================#
.apply:
  stage: deploy
  variables:
    TF_ROOT: ./
    TF_STATE_NAME: default
  cache:
    key: "${TF_ROOT}"
    paths:
      - ${TF_ROOT}/.terraform/
      - ${TF_ROOT}/.terraform.lock.hcl
#  before_script:
#    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
#    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_INFRA_SERVICE_ACCOUNT_BASE64 | base64 -d)
  resource_group: ${TF_STATE_NAME}
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual

.apply.with.var.file:
  extends: .apply
  script:
    # plan_cache="plan.cache"
    # terraform "${@}" -input=false "${plan_cache}"
    - gitlab-terraform apply

.apply.without.var.file:
  extends: .apply
  script:
    - cd ${TF_ROOT}
    # plan_cache="plan.cache"
    # terraform "${@}" -input=false "${plan_cache}"
    - gitlab-terraform apply

#============================#
# .destroy                   #
#============================#
#.destroy:
#  stage: cleanup
#  variables:
#    TF_ROOT: ./
#    TF_STATE_NAME: default
#  cache:
#    key: "${TF_ROOT}"
#    paths:
#      - ${TF_ROOT}/.terraform/
#      - ${TF_ROOT}/.terraform.lock.hcl
##  before_script:
##    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
##    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_INFRA_SERVICE_ACCOUNT_BASE64 | base64 -d)
##    - cd ${TF_ROOT}
##    - cat $TERRAFORM_VAR_FILE_BASE64_DEV | base64 -d > var_file
#  script:
#    # terraform "${@}" -auto-approve
#    - gitlab-terraform destroy -var-file=var_file
#  resource_group: ${TF_STATE_NAME}
#  rules:
#    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
#      when: manual

.destroy:
  stage: cleanup
  variables:
    TF_ROOT: ./
    TF_STATE_NAME: default
  cache:
    key: "${TF_ROOT}"
    paths:
      - ${TF_ROOT}/.terraform/
      - ${TF_ROOT}/.terraform.lock.hcl
  #  before_script:
  #    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
  #    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_INFRA_SERVICE_ACCOUNT_BASE64 | base64 -d)
  #    - cd ${TF_ROOT}
  #    - cat $TERRAFORM_VAR_FILE_BASE64_DEV | base64 -d > var_file

  resource_group: ${TF_STATE_NAME}
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual

.destroy.with.var.file:
  extends: .destroy
  script:
    # terraform "${@}" -auto-approve
    - gitlab-terraform destroy -var-file=var_file

.destroy.without.var.file:
  extends: .destroy
  script:
    - cd ${TF_ROOT}
    # terraform "${@}" -auto-approve
    - gitlab-terraform destroy


#===========================================================#
variables:
  DEV_RES_DIR: EnvDev/1-Res
  DEV_RES_TF_STATE_NAME: "Dev-Resources"
  DEV_VPC_DIR: EnvDev/2-VPC
  DEV_VPC_TF_STATE_NAME: "Dev-VPC"
  DEV_DB_DIR: EnvDev/3-Database
  DEV_DB_TF_STATE_NAME: "Dev-Database"
  DEV_GKE_DIR: EnvDev/4-GKE
  DEV_GKE_TF_STATE_NAME: "Dev-GKE"
  DEV_APPS_DIR: EnvDev/5-Apps
  DEV_APPS_TF_STATE_NAME: "Dev-Apps"
  DEV_GKE_REL_DIR: EnvDev/6-GKE-Rel
  DEV_GKE_REL_TF_STATE_NAME: "Dev-GKE-Rel"
  DEV_APPS_REL_DIR: EnvDev/7-Apps-Rel
  DEV_APPS_REL_TF_STATE_NAME: "Dev-Apps-Rel"

  QA_RES_DIR: EnvQA/1-Res
  QA_RES_TF_STATE_NAME: "QA-Res"
  QA_VPC_DIR: EnvQA/2-VPC
  QA_VPC_TF_STATE_NAME: "QA-VPC"
  QA_DB_DIR: EnvQA/3-Database
  QA_DB_TF_STATE_NAME: "QA-DB"
  QA_GKE_DIR: EnvQA/4-GKE
  QA_GKE_TF_STATE_NAME: "QA-GKE"
  QA_APPS_DIR: EnvQA/5-Apps
  QA_APPS_TF_STATE_NAME: "QA-Apps"
  QA_GKE_London_Dir: EnvQA/6-GKE-London
  QA_GKE_London_TF_State_Name: "QA-GKE-London"
  QA_APPS_London_Dir: EnvQA/7-Apps-London
  QA_APPS_London_TF_State_Name: "QA-Apps-London"
  QA_GKE_Singapore_Dir: EnvQA/8-GKE-Singapore
  QA_GKE_Singapore_TF_State_Name: "QA-GKE-Singapore"
  QA_APPS_Singapore_Dir: EnvQA/9-Apps-Singapore
  QA_APPS_Singapore_TF_State_Name: "QA-Apps-Singapore"
  QA_GKE_LosAngeles_Dir: EnvQA/10-GKE-LosAngeles
  QA_GKE_LosAngeles_TF_State_Name: "QA-GKE-LosAngeles"
  QA_APPS_LosAngeles_Dir: EnvQA/11-Apps-LosAngeles
  QA_APPS_LosAngeles_TF_State_Name: "QA-Apps-LosAngeles"
  QA_LB_Dir: EnvQA/12-LoadBalancer
  QA_LB_TF_State_Name: "QA-LB"
  
  PROD_REVIEW_RES_DIR: EnvProdReview/1-Res
  PROD_REVIEW_RES_TF_STATE_NAME: "Prod-Review-Res"
  PROD_REVIEW_VPC_DIR: EnvProdReview/2-VPC
  PROD_REVIEW_VPC_TF_STATE_NAME: "Prod-Review-VPC"
  PROD_REVIEW_DB_DIR: EnvProdReview/3-Database
  PROD_REVIEW_DB_TF_STATE_NAME: "Prod-Review-DB"
  PROD_REVIEW_GKE_DIR: EnvProdReview/4-GKE
  PROD_REVIEW_GKE_TF_STATE_NAME: "Prod-Review-GKE"
  PROD_REVIEW_APPS_DIR: EnvProdReview/5-Apps
  PROD_REVIEW_APPS_TF_STATE_NAME: "Prod-Review-Apps"
  PROD_REVIEW_GKE_GLOBAL_DIR: EnvProdReview/6-GKE-Global
  PROD_REVIEW_GKE_GLOBAL_TF_STATE_NAME: "Prod-Review-GKE-Global"
  PROD_REVIEW_APPS_GLOBAL_DIR: EnvProdReview/7-Apps-Global
  PROD_REVIEW_APPS_GLOBAL_TF_STATE_NAME: "Prod-Review-Apps-Global"
  PROD_REVIEW_LB_DIR: EnvProdReview/8-LoadBalancer
  PROD_REVIEW_LB_TF_STATE_NAME: "Prod-Review-LB"


  PROD_RES_DIR: EnvProd/1-Res
  PROD_RES_TF_STATE_NAME: "Prod-Res"
  PROD_VPC_DIR: EnvProd/2-VPC
  PROD_VPC_TF_STATE_NAME: "Prod-VPC"
  PROD_DB_DIR: EnvProd/3-Database
  PROD_DB_TF_STATE_NAME: "Prod-DB"
  PROD_GKE_DIR: EnvProd/4-GKE
  PROD_GKE_TF_STATE_NAME: "Prod-GKE"
  PROD_APPS_DIR: EnvProd/5-Apps
  PROD_APPS_TF_STATE_NAME: "Prod-Apps"
  PROD_GKE_LondonDir: EnvProd/6-GKE-London
  PROD_GKE_London_TF_StateName: "Prod-GKE-London"
  PROD_Apps_LondonDir: EnvProd/7-Apps-London
  PROD_Apps_London_TF_StateName: "Prod-Apps-London"
  PROD_LB_DIR: EnvProd/8-LoadBalancer
  PROD_LB_TF_STATE_NAME: "Prod-LB"


# TF_ADDRESS: ${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state/sre-infra
# ${CI_API_V4_URL} = https://gitlab.com/api/v4
# ${CI_PROJECT_ID} = 27641226


#============================#
# Dev - Resources            #
#============================#
Dev-1-Res-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_RES_DIR}
    TF_STATE_NAME: ${DEV_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)

Dev-1-Res-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-1-Res-Plan]
  variables:
    TF_ROOT: ${DEV_RES_DIR}
    TF_STATE_NAME: ${DEV_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)

Dev-1-Res-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-1-Res-Plan]
  variables:
    TF_ROOT: ${DEV_RES_DIR}
    TF_STATE_NAME: ${DEV_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)

#============================#
# Dev - VPC                  #
#============================#
Dev-2-VPC-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_VPC_DIR}
    TF_STATE_NAME: ${DEV_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)

Dev-2-VPC-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-2-VPC-Plan]
  variables:
    TF_ROOT: ${DEV_VPC_DIR}
    TF_STATE_NAME: ${DEV_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)

Dev-2-VPC-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-2-VPC-Plan]
  variables:
    TF_ROOT: ${DEV_VPC_DIR}
    TF_STATE_NAME: ${DEV_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)

#============================#
# Dev - Database             #
#============================#
Dev-3-Database-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_DB_DIR}
    TF_STATE_NAME: ${DEV_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_DB_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-3-Database-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-3-Database-Plan]
  variables:
    TF_ROOT: ${DEV_DB_DIR}
    TF_STATE_NAME: ${DEV_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_DB_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-3-Database-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-3-Database-Plan]
  variables:
    TF_ROOT: ${DEV_DB_DIR}
    TF_STATE_NAME: ${DEV_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_DB_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

#============================#
# Dev - GKE(TW)              #
#============================#
Dev-4-GKE-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_GKE_DIR}
    TF_STATE_NAME: ${DEV_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-4-GKE-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-4-GKE-Plan]
  variables:
    TF_ROOT: ${DEV_GKE_DIR}
    TF_STATE_NAME: ${DEV_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-4-GKE-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-4-GKE-Plan]
  variables:
    TF_ROOT: ${DEV_GKE_DIR}
    TF_STATE_NAME: ${DEV_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

#============================#
# Dev - Apps(TW)             #
#============================#
Dev-5-Apps-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_APPS_DIR}
    TF_STATE_NAME: ${DEV_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

Dev-5-Apps-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-5-Apps-Plan]
  variables:
    TF_ROOT: ${DEV_APPS_DIR}
    TF_STATE_NAME: ${DEV_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

Dev-5-Apps-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-5-Apps-Plan]
  variables:
    TF_ROOT: ${DEV_APPS_DIR}
    TF_STATE_NAME: ${DEV_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1


#============================#
# Dev - GKE(Release)         #
#============================#
Dev-6-GKE-Plan-Rel:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_GKE_REL_DIR}
    TF_STATE_NAME: ${DEV_GKE_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-6-GKE-Apply-Rel:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-6-GKE-Plan-Rel]
  variables:
    TF_ROOT: ${DEV_GKE_REL_DIR}
    TF_STATE_NAME: ${DEV_GKE_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

Dev-6-GKE-Destroy-Rel:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-6-GKE-Plan-Rel]
  variables:
    TF_ROOT: ${DEV_GKE_REL_DIR}
    TF_STATE_NAME: ${DEV_GKE_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

#============================#
# Dev - Apps(Release)        #
#============================#
Dev-7-Apps-Plan-Rel:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: ${DEV_APPS_REL_DIR}
    TF_STATE_NAME: ${DEV_APPS_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

Dev-7-Apps-Apply-Rel:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-7-Apps-Plan-Rel]
  variables:
    TF_ROOT: ${DEV_APPS_REL_DIR}
    TF_STATE_NAME: ${DEV_APPS_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

Dev-7-Apps-Destroy-Rel:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [Dev-7-Apps-Plan-Rel]
  variables:
    TF_ROOT: ${DEV_APPS_REL_DIR}
    TF_STATE_NAME: ${DEV_APPS_REL_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $DEV_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $DEV_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1















#============================#
# QA - Resources             #
#============================#
QA-1-Res-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_RES_DIR
    TF_STATE_NAME: $QA_RES_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

QA-1-Res-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-1-Res-Plan]
  variables:
    TF_ROOT: $QA_RES_DIR
    TF_STATE_NAME: $QA_RES_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

QA-1-Res-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-1-Res-Plan]
  variables:
    TF_ROOT: $QA_RES_DIR
    TF_STATE_NAME: $QA_RES_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

#============================#
# QA - VPC                   #
#============================#
QA-2-VPC-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_VPC_DIR
    TF_STATE_NAME: $QA_VPC_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

QA-2-VPC-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-2-VPC-Plan]
  variables:
    TF_ROOT: $QA_VPC_DIR
    TF_STATE_NAME: $QA_VPC_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

QA-2-VPC-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-2-VPC-Plan]
  variables:
    TF_ROOT: $QA_VPC_DIR
    TF_STATE_NAME: $QA_VPC_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual

#============================#
# QA - Database              #
#============================#
QA-3-DB-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_DB_DIR
    TF_STATE_NAME: $QA_DB_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-3-DB-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-3-DB-Plan]
  variables:
    TF_ROOT: $QA_DB_DIR
    TF_STATE_NAME: $QA_DB_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-3-DB-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-3-DB-Plan]
  variables:
    TF_ROOT: $QA_DB_DIR
    TF_STATE_NAME: $QA_DB_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

#============================#
# QA - GKE                   #
#============================#
QA-4-GKE-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_GKE_DIR
    TF_STATE_NAME: $QA_GKE_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-4-GKE-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-4-GKE-Plan]
  variables:
    TF_ROOT: $QA_GKE_DIR
    TF_STATE_NAME: $QA_GKE_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-4-GKE-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-4-GKE-Plan]
  variables:
    TF_ROOT: $QA_GKE_DIR
    TF_STATE_NAME: $QA_GKE_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

#============================#
# QA - Apps                  #
#============================#
QA-5-Apps-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_APPS_DIR
    TF_STATE_NAME: $QA_APPS_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-5-Apps-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-5-Apps-Plan]
  variables:
    TF_ROOT: $QA_APPS_DIR
    TF_STATE_NAME: $QA_APPS_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1

QA-5-Apps-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-5-Apps-Plan]
  variables:
    TF_ROOT: $QA_APPS_DIR
    TF_STATE_NAME: $QA_APPS_TF_STATE_NAME
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
  tags:
    - g1


#=============================#
# QA - GKE(London)
#=============================#
QA-6-GKE-London-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_GKE_London_Dir
    TF_STATE_NAME: $QA_GKE_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-6-GKE-London-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-6-GKE-London-Plan]
  variables:
    TF_ROOT: $QA_GKE_London_Dir
    TF_STATE_NAME: $QA_GKE_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-6-GKE-London-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-6-GKE-London-Plan]
  variables:
    TF_ROOT: $QA_GKE_London_Dir
    TF_STATE_NAME: $QA_GKE_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1



#============================#
# QA - Apps(London)          
#============================#
QA-7-Apps-London-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_APPS_London_Dir
    TF_STATE_NAME: $QA_APPS_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-7-Apps-London-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-7-Apps-London-Plan]
  variables:
    TF_ROOT: $QA_APPS_London_Dir
    TF_STATE_NAME: $QA_APPS_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-7-Apps-London-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-7-Apps-London-Plan]
  variables:
    TF_ROOT: $QA_APPS_London_Dir
    TF_STATE_NAME: $QA_APPS_London_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1



#=============================#
# QA - GKE(Singapore)
#=============================#
QA-8-GKE-Singapore-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_GKE_Singapore_Dir
    TF_STATE_NAME: $QA_GKE_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-8-GKE-Singapore-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-8-GKE-Singapore-Plan]
  variables:
    TF_ROOT: $QA_GKE_Singapore_Dir
    TF_STATE_NAME: $QA_GKE_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-8-GKE-Singapore-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-8-GKE-Singapore-Plan]
  variables:
    TF_ROOT: $QA_GKE_Singapore_Dir
    TF_STATE_NAME: $QA_GKE_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1



#============================#
# QA - Apps(Singapore)
#============================#
QA-9-Apps-Singapore-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_APPS_Singapore_Dir
    TF_STATE_NAME: $QA_APPS_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-9-Apps-Singapore-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-9-Apps-Singapore-Plan]
  variables:
    TF_ROOT: $QA_APPS_Singapore_Dir
    TF_STATE_NAME: $QA_APPS_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-9-Apps-Singapore-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-9-Apps-Singapore-Plan]
  variables:
    TF_ROOT: $QA_APPS_Singapore_Dir
    TF_STATE_NAME: $QA_APPS_Singapore_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1



#=============================#
# QA - GKE(Los Angeles)
#=============================#
QA-10-GKE-LosAngeles-Plan:
  extends: .plan.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_GKE_LosAngeles_Dir
    TF_STATE_NAME: $QA_GKE_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-10-GKE-LosAngeles-Apply:
  extends: .apply.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-10-GKE-LosAngeles-Plan]
  variables:
    TF_ROOT: $QA_GKE_LosAngeles_Dir
    TF_STATE_NAME: $QA_GKE_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1

QA-10-GKE-LosAngeles-Destroy:
  extends: .destroy.without.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-10-GKE-LosAngeles-Plan]
  variables:
    TF_ROOT: $QA_GKE_LosAngeles_Dir
    TF_STATE_NAME: $QA_GKE_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  tags:
    - g1


#============================#
# QA - Apps(Singapore)
#============================#
QA-11-Apps-LosAngeles-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_APPS_LosAngeles_Dir
    TF_STATE_NAME: $QA_APPS_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-11-Apps-LosAngeles-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-11-Apps-LosAngeles-Plan]
  variables:
    TF_ROOT: $QA_APPS_LosAngeles_Dir
    TF_STATE_NAME: $QA_APPS_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-11-Apps-LosAngeles-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-11-Apps-LosAngeles-Plan]
  variables:
    TF_ROOT: $QA_APPS_LosAngeles_Dir
    TF_STATE_NAME: $QA_APPS_LosAngeles_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd $TF_ROOT
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1




#============================#
# QA - LoadBalancer          #
#============================#
QA-12-LB-Plan:
  extends: .plan.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  variables:
    TF_ROOT: $QA_LB_Dir
    TF_STATE_NAME: $QA_LB_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-12-LB-Apply:
  extends: .apply.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-12-LB-Plan]
  variables:
    TF_ROOT: $QA_LB_Dir
    TF_STATE_NAME: $QA_LB_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1

QA-12-LB-Destroy:
  extends: .destroy.with.var.file
  image: registry.gitlab.com/gitlab-org/terraform-images/releases/1.2:v0.43.2
  needs: [QA-12-LB-Plan]
  variables:
    TF_ROOT: $QA_LB_Dir
    TF_STATE_NAME: $QA_LB_TF_State_Name
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  tags:
    - g1
















#============================#
# Prod - Resources           #
#============================#
Prod-1-Res-Plan:
  extends: .plan.without.var.file
  variables:
    TF_ROOT: ${PROD_RES_DIR}
    TF_STATE_NAME: ${PROD_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual

Prod-1-Res-Apply:
  extends: .apply.without.var.file
  needs: [Prod-1-Res-Plan]
  variables:
    TF_ROOT: ${PROD_RES_DIR}
    TF_STATE_NAME: ${PROD_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual

Prod-1-Res-Destroy:
  extends: .destroy.without.var.file
  needs: [Prod-1-Res-Plan]
  variables:
    TF_ROOT: ${PROD_RES_DIR}
    TF_STATE_NAME: ${PROD_RES_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_RES_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual




#============================#
# Prod - VPC                 #
#============================#
Prod-2-VPC-Plan:
  extends: .plan.without.var.file
  variables:
    TF_ROOT: ${PROD_VPC_DIR}
    TF_STATE_NAME: ${PROD_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual

Prod-2-VPC-Apply:
  extends: .apply.without.var.file
  needs: [Prod-2-VPC-Plan]
  variables:
    TF_ROOT: ${PROD_VPC_DIR}
    TF_STATE_NAME: ${PROD_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual

Prod-2-VPC-Destroy:
  extends: .destroy.without.var.file
  needs: [Prod-2-VPC-Plan]
  variables:
    TF_ROOT: ${PROD_VPC_DIR}
    TF_STATE_NAME: ${PROD_VPC_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_VPC_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual


#====================#
# Prod - Database    #
#====================#
Prod-3-DB-Plan:
  extends: .plan.with.var.file
  variables:
    TF_ROOT: ${PROD_DB_DIR}
    TF_STATE_NAME: ${PROD_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_DB_TF_VAR_FILE_BASE64 | base64 -d > var_file 
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-3-DB-Apply:
  extends: .apply.with.var.file
  needs: [Prod-3-DB-Plan]
  variables:
    TF_ROOT: ${PROD_DB_DIR}
    TF_STATE_NAME: ${PROD_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_DB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1


Prod-3-DB-Destroy:
  extends: .destroy.with.var.file
  needs: [Prod-3-DB-Plan]
  variables:
    TF_ROOT: ${PROD_DB_DIR}
    TF_STATE_NAME: ${PROD_DB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_DB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_DB_TF_VAR_FILE_BASE64 | base64 -d > var_file     
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1




#====================#
# Prod - GKE         #
#====================#
Prod-4-GKE-Plan:
  extends: .plan.without.var.file
  variables:
    TF_ROOT: ${PROD_GKE_DIR}
    TF_STATE_NAME: ${PROD_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-4-GKE-Apply:
  extends: .apply.without.var.file
  needs: [Prod-4-GKE-Plan]
  variables:
    TF_ROOT: ${PROD_GKE_DIR}
    TF_STATE_NAME: ${PROD_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-4-GKE-Destroy:
  extends: .destroy.without.var.file
  needs: [Prod-4-GKE-Plan]
  variables:
    TF_ROOT: ${PROD_GKE_DIR}
    TF_STATE_NAME: ${PROD_GKE_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1


#============================#
# Prod - Apps                #
#============================#
Prod-5-Apps-Plan:
  extends: .plan.with.var.file
  variables:
    TF_ROOT: ${PROD_APPS_DIR}
    TF_STATE_NAME: ${PROD_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1
  

Prod-5-Apps-Apply:
  extends: .apply.with.var.file
  needs: [Prod-5-Apps-Plan]
  variables:
    TF_ROOT: ${PROD_APPS_DIR}
    TF_STATE_NAME: ${PROD_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-5-Apps-Destroy:
  extends: .destroy.with.var.file
  needs: [Prod-5-Apps-Plan]
  variables:
    TF_ROOT: ${PROD_APPS_DIR}
    TF_STATE_NAME: ${PROD_APPS_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1


#============================#
# Prod - GKE(London)         #
#============================#
Prod-6-GKE-London-Plan:
  extends: .plan.without.var.file
  variables:
    TF_ROOT: $PROD_GKE_LondonDir
    TF_STATE_NAME: $PROD_GKE_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-6-GKE-London-Apply:
  extends: .apply.without.var.file
  needs: [Prod-6-GKE-London-Plan]
  variables:
    TF_ROOT: $PROD_GKE_LondonDir
    TF_STATE_NAME: $PROD_GKE_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-6-GKE-London-Destroy:
  extends: .destroy.without.var.file
  needs: [Prod-6-GKE-London-Plan]
  variables:
    TF_ROOT: $PROD_GKE_LondonDir
    TF_STATE_NAME: $PROD_GKE_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_GKE_SERVICE_ACCOUNT_BASE64 | base64 -d)
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1


#=============================#
# Prod - Apps(London)
#=============================#
Prod-7-Apps-London-Plan:
  extends: .plan.with.var.file
  variables:
    TF_ROOT: $PROD_Apps_LondonDir
    TF_STATE_NAME: $PROD_Apps_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-7-Apps-London-Apply:
  extends: .apply.with.var.file
  needs: [Prod-7-Apps-London-Plan]
  variables:
    TF_ROOT: $PROD_Apps_LondonDir
    TF_STATE_NAME: $PROD_Apps_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-7-Apps-London-Destroy:
  extends: .destroy.with.var.file
  needs: [Prod-7-Apps-London-Plan]
  variables:
    TF_ROOT: $PROD_Apps_LondonDir
    TF_STATE_NAME: $PROD_Apps_London_TF_StateName
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_APPS_SERVICE_ACCOUNT_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_APPS_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1


#==============================#
# Prod - LoadBalancer          #
#==============================#
Prod-8-LB-Plan:
  extends: .plan.with.var.file
  variables:
    TF_ROOT: ${PROD_LB_DIR}
    TF_STATE_NAME: ${PROD_LB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-8-LB-Apply:
  extends: .apply.with.var.file
  needs: [Prod-8-LB-Plan]
  variables:
    TF_ROOT: ${PROD_LB_DIR}
    TF_STATE_NAME: ${PROD_LB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1

Prod-8-LB-Destroy:
  extends: .destroy.with.var.file
  needs: [Prod-8-LB-Plan]
  variables:
    TF_ROOT: ${PROD_LB_DIR}
    TF_STATE_NAME: ${PROD_LB_TF_STATE_NAME}
  before_script:
    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_LB_SA_BASE64 | base64 -d)
    - cd ${TF_ROOT}
    - cat $PROD_LB_TF_VAR_FILE_BASE64 | base64 -d > var_file
  rules:
    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
      when: manual
  tags:
    - g1






























#============================#
# QA - Cloud Run             #
#============================#
#QA-6-CloudRun-Plan:
#  extends: .plan.without.var.file
#  variables:
#    TF_ROOT: ${QA_CLOUD_RUN_DIR}
#    TF_STATE_NAME: ${QA_CLOUD_RUN_TF_STATE_NAME}
#  before_script:
#    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
#    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_CLOUD_RUN_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
#
#QA-6-CloudRun-Apply:
#  extends: .apply.without.var.file
#  needs: [QA-6-CloudRun-Plan]
#  variables:
#    TF_ROOT: ${QA_CLOUD_RUN_DIR}
#    TF_STATE_NAME: ${QA_CLOUD_RUN_TF_STATE_NAME}
#  before_script:
#    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
#    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_CLOUD_RUN_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual
#
#QA-6-CloudRun-Destroy:
#  extends: .destroy.without.var.file
#  needs: [QA-6-CloudRun-Plan]
#  variables:
#    TF_ROOT: ${QA_CLOUD_RUN_DIR}
#    TF_STATE_NAME: ${QA_CLOUD_RUN_TF_STATE_NAME}
#  before_script:
#    # https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#full-reference
#    - export GOOGLE_CLOUD_KEYFILE_JSON=$(cat $PROD_CLOUD_RUN_SA_BASE64 | base64 -d)
#  rules:
#    - if: $CI_COMMIT_TAG # Run this job when a tag is created manually.
#      when: manual










