apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Godaddy.fullname" . }}-test-connection"
  labels:
    {{- include "Godaddy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Godaddy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
